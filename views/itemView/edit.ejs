<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit <%= item.name %></title>
    <link rel="stylesheet" href="/style.css">
</head>
<body>
    <a href="/items/<%= item._id %>" class="back-button">← Back</a>
    <div class="container">
        <h1>Edit <%= item.name %></h1>
        <form action="/items/<%= item._id %>?_method=PUT" method="POST">

                <label for="name">Name:</label>
                <input type="text" id="name" name="name" value="<%= item.name %>" required>

            

                <label for="category">Category:</label>
                <input type="text" id="category" name="category" value="<%= item.category %>">

            
                <label for="weight">Weight (kg):</label>
                <input type="number" id="weight" name="weight" value="<%= item.weight %>" step="0.1">
            
                <label for="size">Size:</label>
                <select id="size" name="size">
                    <option value="Small" <%= item.size === 'Small' ? 'selected' : '' %>>Small</option>
                    <option value="Medium" <%= item.size === 'Medium' ? 'selected' : '' %>>Medium</option>
                    <option value="Large" <%= item.size === 'Large' ? 'selected' : '' %>>Large</option>
                </select>
            
                <label for="lastSeen">Last Seen:</label>
                <input type="text" id="lastSeen" name="lastSeen" value="<%= item.lastSeen %>">
            
                <label>
                    <input type="checkbox" name="isDailyUse" <%= item.isDailyUse ? 'checked' : '' %>>
                    Daily Use
                </label>
            
                <label>
                    <input type="checkbox" name="fraglie" <%= item.fraglie ? 'checked' : '' %>>
                    Fragile
                </label>            
            <button type="submit" class="link">Save Changes</button>
        </form>
    </div>
</body>
</html>
</html>
